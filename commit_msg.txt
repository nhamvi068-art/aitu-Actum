feat: add GitHub Actions CI/CD workflows for Vercel deployment

- Add .github/workflows/ci.yml: lint, typecheck, build, smoke tests, and Vercel preview/prod deployment
- Add .github/workflows/vercel-deploy.yml: standalone Vercel deploy workflow with workflow_dispatch support
- Remove .github/ from .gitignore to allow workflow files to be tracked
- vercel.json was already correctly configured with build command, output directory, rewrites, and redirects
