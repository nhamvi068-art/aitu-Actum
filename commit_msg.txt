refactor: replace blt GPT adapter with updated tuzi GPT adapter

- Replace tuzi-gpt-image-adapter.ts with opentu-main version:
  response_format and quality only written when present
- Remove blt-image-adapter.ts (no longer used)
- Update unit tests to match new body construction logic
