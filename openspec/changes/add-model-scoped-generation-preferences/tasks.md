# Tasks: 按模型记忆生成参数偏好

## 1. 存储层

- [x] 1.1 为图片、视频、音频增加按 `selectionKey || modelId` 作用域存储的偏好结构
- [x] 1.2 补充统一的读写、兼容过滤、默认值回退工具函数
- [x] 1.3 保持现有偏好存储兼容，避免首次升级时报错

## 2. 图片生成

- [x] 2.1 在图片模型切换时优先回填该模型的用户偏好
- [x] 2.2 保持 `aspectRatio`、额外参数与模型兼容性过滤一致
- [x] 2.3 确保编辑历史任务与外部初始化参数优先级高于本地偏好

## 3. 视频生成

- [x] 3.1 在视频模型切换时优先回填该模型的 `duration`、`size`、额外参数
- [x] 3.2 保持 storyboard、图片数量裁剪等现有模型能力约束不变
- [x] 3.3 确保编辑历史任务与外部初始化参数优先级高于本地偏好

## 4. 音频生成

- [x] 4.1 为音频模型切换接入按模型参数偏好回填
- [x] 4.2 保持强制参数与兼容参数过滤逻辑不变

## 5. 验证

- [x] 5.1 为偏好服务补充模型作用域相关测试
- [ ] 5.2 手动验证图片、视频、音频在模型切换后均能回填用户偏好
- [ ] 5.3 验证同名模型来自不同供应商时参数不会串用
