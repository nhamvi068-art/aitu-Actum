## 1. 共享内核
- [x] 1.1 设计备份导出/导入的共享能力接口与数据模型
- [x] 1.2 抽离 manifest、素材命名、任务筛选、分片与二进制条目解析为共享实现
- [x] 1.3 抽离项目与知识库导出/导入编排为共享实现

## 2. 主应用接入
- [x] 2.1 为主应用实现存储访问与 UI 进度适配层
- [x] 2.2 用共享内核替换主应用现有备份导出逻辑
- [x] 2.3 用共享内核替换主应用现有备份导入逻辑

## 3. sw-debug 接入
- [x] 3.1 为 `sw-debug` 实现 IndexedDB/Cache Storage 适配层
- [x] 3.2 用共享内核替换 `sw-debug` 现有备份导出逻辑
- [x] 3.3 用共享内核替换 `sw-debug` 现有备份导入逻辑

## 4. 验证
- [x] 4.1 补充共享内核单测，覆盖音频、多结果任务、知识库、文件命名冲突
- [x] 4.2 补充环境适配层回归测试或最小集成测试
- [x] 4.3 校验主应用与 `sw-debug` 的导出结构和恢复结果一致
